interface CanvasType {
  draw(): void;
  drawCircle(x: number, y: number, radius: number, isHalf: boolean): void;
  keepCanvasStatus(callback: () => void): void;
}


export class Constellation implements CanvasType {
  constructor() {

  }
  

  drawCircle(x: number, y: number, radius: number, isHalf: boolean): void {
    
  }

  keepCanvasStatus(callback: () => void): void {
    
  }

  draw(): void {
    
  }
}
